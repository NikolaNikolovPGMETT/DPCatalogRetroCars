namespace DPCatalogRetroCars.Models;

public enum StoryStatus
{
    Pending = 0,
    Approved = 1,
    Removed = 2
}

public enum RestorationStatus
{
    Current = 0,
    Future = 1,
    Completed = 2
}